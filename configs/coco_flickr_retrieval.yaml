
pretrained_path: /workspace/models/

model_version:
model_name:

data:
  shared_args:
    batch_size: 256
    num_workers: 4
    shuffle: False
    drop_last: False

    data_path: /workspace
    num_max_bpe_tokens: 64

  datamodules:
    coco_captions:
      task: captioning
      color_jitter: # None
      beit_transforms: False
      crop_scale: [1.0, 1.0]

    flickr30:
      color_jitter: # None
      beit_transforms: False
      crop_scale: [1.0, 1.0]
